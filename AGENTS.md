# AGENTS.md

本仓库的 AI 协作规则只有一个权威来源：`CLAUDE.md`。

所有代理在开始工作前都应先阅读根目录的 `CLAUDE.md`。

执行规则：

- 仓库级架构说明、开源边界、修改范围、验证要求，全部以 `CLAUDE.md` 为准
- 如果 `README`、旧注释、历史文档或本文件与 `CLAUDE.md` 冲突，以 `CLAUDE.md` 为准
- 如果未来某个子目录新增自己的 `CLAUDE.md`，它只能补充该子树的实现细节；仓库级规则仍以根 `CLAUDE.md` 为准
